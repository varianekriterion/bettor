# Base URL only — do not append /api/v1 (see src/lib/api.ts)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase (Auth + bet_journal). Enable Email provider in Dashboard → Authentication → Providers
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
