fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
playwright==1.49.1
pandas==2.2.3
numpy==1.26.4  # capped <2.0 — langchain==0.3.7 requires numpy<2.0.0; nothing in this codebase needs numpy 2.x
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
supabase==2.11.0
tenacity==9.0.0
cachetools==5.5.0
aiosqlite==0.20.0
apscheduler==3.10.4
pytest==8.3.4
pytest-asyncio==0.25.0

# --- AI / RAG / xG data (Phase 1-3) ---
# NOTE: understat==0.1.14 is intentionally NOT listed here. Its packaging
# mistakenly declares pytest==7.2.0 (+ pytest-aiohttp/pytest-cov/pytest-mock)
# as a RUNTIME install_requires rather than a dev/test-only extra, which
# conflicts with this project's own pytest pin no matter what version you
# choose — pip's resolver will hard-fail with "ResolutionImpossible" if you
# add it to this file normally. Its only actual runtime dependency is
# aiohttp (already pinned below), so install it separately with --no-deps
# AFTER the rest of this file:
#   pip install -r requirements.txt
#   pip install --no-deps understat==0.1.14
aiohttp==3.11.11          # session transport required by understat
openai==1.57.0            # embeddings (text-embedding-3-small) + chat fallback
langchain==0.3.7
langchain-openai==0.2.9
# langchain-core intentionally unpinned: langchain==0.3.7 needs >=0.3.15,<0.4.0
# and langchain-openai==0.2.9 needs >=0.3.17,<0.4.0 — pinning it ourselves to
# any single value fights one of the two. Let pip resolve it transitively.
# Pin versions above loosely-verified; run `pip install -U ... && pip freeze` before deploying.
